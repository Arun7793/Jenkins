pipeline {
    agent any


    stages {
        stage ('build') {
                when {
                    branch 'main'

                }
                steps {
                    echo "run only if its a main branch"
                }
            }
        }
    }
