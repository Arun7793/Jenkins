pipeline {
    agent any


    stages {
        stage ('build') {
                when {
                    branch 'master'

                }
                steps {
                    echo "run only if its a main branch"
                }
            }
        }
    }
