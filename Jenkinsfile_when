pipeline {
    agent any


    stages {
        stage ('build') {
            stage {
                when {
                    branch 'main'

                }
                steps {
                    echo "run only if its a main branch"
                }
            }
        }
    }
}
